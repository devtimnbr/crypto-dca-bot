
services:
  crypto-dca-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - STATE_DIR=/app/data
    volumes:
      - dca-bot-data:/app/data

volumes:
  dca-bot-data:

